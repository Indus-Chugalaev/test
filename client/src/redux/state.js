import { useState, useCallback, useEffect } from 'react'
import { useHttp } from '../hooks/http.hook'
import { useAuth } from '../hooks/auth.hook'


export const state2 = () => {



   // const { token, login, logout, userId, userEmail, userRole, ready } = useAuth()
   // // start state
   // // start {products}
   // const [products, setProducts] = useState([])
   // const { request } = useHttp()
   // const fetchProducts = useCallback(async () => {
   //    try {
   //       const fetched = await request('/api/product', 'GET', null, {
   //          Authorization: `Bearer ${token}`
   //       })
   //       setProducts(fetched)
   //    } catch (e) { }
   // }, [token, request])
   // useEffect(() => {
   //    fetchProducts()
   // }, [fetchProducts])
   // // end {products}


   // // start {services}
   // const [services, setServices] = useState([])
   // const fetchServices = useCallback(async () => {
   //    try {
   //       const fetched = await request('/api/service', 'GET', null, {
   //          Authorization: `Bearer ${token}`
   //       })
   //       setServices(fetched)
   //    } catch (e) { }
   // }, [token, request])
   // useEffect(() => {
   //    fetchServices()
   // }, [fetchServices])
   // // end {services}

   // // start {clients}
   // const [clients, setClients] = useState([])
   // const fetchClients = useCallback(async () => {
   //    try {
   //       const fetched = await request('/api/user', 'GET', null, {
   //          Authorization: `Bearer ${token}`
   //       })
   //       setClients(fetched)
   //    } catch (e) { }
   // }, [token, request])

   // useEffect(() => {
   //    fetchClients()
   // }, [fetchClients])
   // // end {clients}

   // // start {orders}
   // const [orders, setOrders] = useState([])
   // const fetchOrders = useCallback(async () => {
   //    try {
   //       const fetched = await request('/api/order', 'GET', null, {
   //          Authorization: `Bearer ${token}`
   //       })
   //       setOrders(fetched)
   //    } catch (e) { }
   // }, [token, request])
   // useEffect(() => {
   //    fetchOrders()
   // }, [fetchOrders])
   // // end {orders}

   // // start {appointments}
   // const [appointments, setAppointments] = useState([])
   // const fetchAppointments = useCallback(async () => {
   //    try {
   //       const fetched = await request('/api/appointment', 'GET', null, {
   //          Authorization: `Bearer ${token}`
   //       })
   //       setAppointments(fetched)
   //    } catch (e) { }
   // }, [token, request])
   // useEffect(() => {
   //    fetchAppointments()
   // }, [fetchAppointments])
   // // end {appointments}

   // // start {carts}
   // const [carts, setCarts] = useState([])
   // const fetchCarts = useCallback(async () => {
   //    try {
   //       const fetched = await request('/api/cart', 'GET', null, {
   //          Authorization: `Bearer ${token}`
   //       })
   //       setCarts(fetched)
   //    } catch (e) { }
   // }, [token, request])
   // useEffect(() => {
   //    fetchCarts()
   // }, [fetchCarts])
   // // end {carts}

   // // start {user}
   // const [user, setUser] = useState([])
   // // const userId = user._id
   // const fetchUser = useCallback(async () => {
   //    try {
   //       const fetched = await request(`/api/profile/${userId}`, 'GET', null, {
   //          Authorization: `Bearer ${token}`
   //       })
   //       setUser(fetched)
   //    } catch (e) { }
   // }, [token, request, userId])
   // useEffect(() => {
   //    fetchUser()
   // }, [fetchUser])
   // // end {user}

   // console.log(5);

   // console.log(products);
   // // console.log(state2);
   // //  return { products, services, clients, orders, appointments, carts }
   // // end state


   // console.log(3);
}

// const state = this.state2
// console.log(state);
//    )

// }
// export let state2 = State1
// ({
//    products
//    // services, clients, orders, appointments, carts, user 
// })

export const state1 = 44

let a = 25

export default a
